# خطة الجاهزية للإنتاج — Radeef HRMS

## حالة التنفيذ (محدَّثة 2026-09-23)

**الكود جاهز للإنتاج ومُختبر فعلياً. ما يبقى هو خطوات على الخادم لا يمكن تنفيذها من المستودع.**

### ما تم التحقق منه على قاعدة بيانات PostgreSQL حقيقية

| الفحص | النتيجة |
|---|---|
| أخطاء TypeScript | 0، والبناء لا يتجاهل الأخطاء بعد الآن |
| ESLint على 280 ملف | 0 أخطاء و0 تحذيرات، و`any` صار ممنوعاً |
| اختبارات الوحدة | 362 ناجحة |
| بناء الإنتاج `next build` | ناجح بلا تحذيرات |
| فحص كل صفحة ومسار API على نسخة الإنتاج | 143 من 144، والوحيد المتبقي سلوك صحيح |
| تدفقات أعمال حقيقية عبر HTTP | قرابة 250 تدفقاً في ثلاث جولات |
| مصفوفة الصلاحيات: 11 دوراً × كل المسارات | بلا تسريب بيانات |
| زحف متصفح حقيقي لكل الصفحات بكل الأدوار | بلا أخطاء خادم أو انهيار صفحات |
| تبنّي الترحيلات على قاعدة قديمة أُنشئت بـ `db push` | ناجح بلا انحراف |

### ما تم إنجازه من الخطة

- **المرحلة 0 و1 (الأمان):** جلسات موقّعة httpOnly، وحماية كل المسارات بالأدوار، وحذف الحساب السري. كلمات مرور المنصات الحكومية صارت مشفرة، ورفع الملفات مقيّد بالنوع والحجم وملكية الملف. أُضيف حد لمحاولات الدخول لا يمكن تجاوزه بتغيير الترويسات، والخروج يلغي الجلسة من كل الأجهزة، وتغيير كلمة المرور يلغي الجلسات الأخرى. أُضيفت ترويسات الأمان، وحُذفت كل الأسرار من الكود.
- **المرحلة 2 (قاعدة البيانات):** خمسة ترحيلات مراجَعة بلا حذف بيانات، وفهارس لكل المفاتيح الأجنبية، وسياسات حذف آمنة للسجلات المالية. أُضيف سكربت seed، وأداة إنشاء المدير الأول.
- **المرحلة 3 (المنطق المالي):** أقساط السلف تُخصم مرة واحدة وتنتهي، والمكافآت تُصرف، ونهاية الخدمة تُحسب على الخادم وفق نظام العمل. رصيد الإجازات صحيح ولا يُصفَّر، والتأمينات تُحتسب 9.75% للسعوديين، والرواتب تُحتسب نسبياً للمعيّنين والمنتهية خدمتهم أثناء الشهر. العمل الإضافي والخصومات لا تُصرف مرتين، وكل الاعتمادات محمية من التنفيذ المزدوج.
- **المرحلة 4 و5 (الـ API والواجهة):** تحقق zod على كل المدخلات، ورسائل خطأ عربية موحدة، وسجل تدقيق. أُضيفت واجهة ثابتة لا تُعاد في كل تنقّل، وصفحات خطأ عربية، وشاشة "لا صلاحية" بدل الصفحات المكسورة. استُبدل كل `alert` و`confirm`، وصارت التواريخ ميلادية موحدة.
- **المرحلة 6 (النشر):** Dockerfile، وdocker-compose، وملف PM2، وسكربتات نشر بلا توقف ونسخ احتياطي واستعادة ومستأجر جديد. أُضيف قالب Nginx مع TLS وCI على GitHub، وأُعيدت كتابة README. لوحة إدارة المستأجرين أُصلحت من حقن الأوامر، ولم تعد توقف العملاء الحاليين تلقائياً.
- **المرحلة 7 (الاختبارات):** 362 اختبار وحدة، وسكربتات E2E محفوظة.

### ما يجب عليك فعله على الخادم (بالترتيب، التفاصيل في `docs/RUNBOOK.md`)

1. **تدوير كلمة مرور root للخادم** والانتقال لمفاتيح SSH، وتدوير كلمة مرور Postgres. هذه كانت مكشوفة في الكود، وحذفها من الكود لا يلغيها.
2. **ضبط `SESSION_SECRET` و`DATA_ENCRYPTION_KEY`** في ملف بيئة كل مستأجر قبل تشغيل النسخة الجديدة.
3. **نسخة احتياطية يدوية** لكل قاعدة، ثم تبنّي الترحيلات حسب `docs/DATABASE.md`.
4. **إنشاء المدير الأول** بـ `npm run admin:create`، لأن الحساب الثابت القديم حُذف.
5. **نقل الملفات** من `public/uploads` إلى `UPLOAD_DIR`، ثم تشغيل `register-legacy-uploads.mjs` و`encrypt-gov-passwords.mjs`.
6. **تفعيل النسخ الاحتياطي اليومي** مع نسخة خارج الخادم.
7. **تنظيف تاريخ git** إن كان المستودع مرفوعاً على GitHub.

### قرارات تحتاج تأكيدك

- **عرض التواريخ صار ميلادياً** بأرقام لاتينية بدل الهجري المختلط.
- **خصم أيام الإجازة غير المدفوعة يُطبَّق الآن على السعوديين أيضاً** بأجر اليوم الفعلي. كان سابقاً رسماً ثابتاً قدره 30 ريالاً.
- **التأمينات تُخصم تلقائياً 9.75%** من الأساسي والسكن للسعودي، إلا إذا حُدد مبلغ يدوي في ملف الموظف.
- **الجلسة تنتهي بعد 12 ساعة**، ويمكن تغيير ذلك من الإعدادات.

---

**تاريخ المراجعة:** 2026-09-23
**النطاق:** مراجعة كاملة للكود (Next.js 16 / React 19 / Prisma 5.22 / PostgreSQL) عبر 5 مسارات متوازية: الأمان، الـ API ومنطق الأعمال، الواجهة الأمامية، قاعدة البيانات، والنشر والبنية التحتية، إضافة إلى فحص البناء والأنواع والـ lint.

---

## 1. الحكم النهائي

**التطبيق غير جاهز للإنتاج حاليًا، ويجب اعتبار البيئات الثلاث الحية (radeef / rakan / dar) مكشوفة الآن.**

السبب ليس جودة كود منخفضة عمومًا، بل غياب طبقات كاملة: لا مصادقة على الـ API، لا Migrations، لا نسخ احتياطي دوري، وأسرار الإنتاج مكتوبة نصًا داخل المستودع. إضافة إلى أخطاء منطق مالي تؤثر مباشرة على رواتب الموظفين.

### أرقام الفحص الآلي

| الفحص | النتيجة |
|---|---|
| `tsc --noEmit` | 2 أخطاء (مخفية حاليًا بـ `ignoreBuildErrors: true`) |
| `eslint src` | 1315 مشكلة: 982 `any` صريح، 278 متغير غير مستخدم |
| مسارات API بلا أي تحقق من الهوية | 78 من 80 |
| مسارات تُرجع `err.message` الخام للعميل | 74 من 80 |
| `@@index` في مخطط Prisma | 0 |
| مجلد `prisma/migrations` | غير موجود |
| ملفات تحتوي كلمة مرور root للسيرفر | 19 |
| `alert()` / `confirm()` في الواجهة | 205 / 35 |
| حجم الكود (src) | ~41,000 سطر، 90 صفحة، 80 مسار API، 53 موديل |

---

## 2. الاكتشافات الحرجة (يجب إصلاحها قبل أي شيء آخر)

### 2.1 الأمان

| # | الاكتشاف | الموقع | الأثر |
|---|---|---|---|
| S1 | الـ middleware يستثني `/api` كليًا، ولا يوجد أي تحقق من الهوية أو الدور داخل المسارات | `src/middleware.ts:31` وكل `src/app/api/**` | أي شخص على الإنترنت ينشئ SUPER_ADMIN، يولّد رواتب، يعتمد مدفوعات، ويسحب بيانات الموظفين |
| S2 | Backdoor ثابت `admin@radeef.com / 123456` + قبول كلمة المرور نصًا إن ساوت `passwordHash` + cookie `userId` غير httpOnly | `src/app/api/auth/login/route.ts:12-25, 43, 64` | دخول بلا حساب، وانتحال أي مستخدم بتعديل cookie |
| S3 | الدور محفوظ في `localStorage` مع زر "تجربة الأدوار" في القائمة يختار ADMIN | `src/context/RoleContext.tsx`, `src/components/DashboardLayout.tsx:552-569` | أي موظف يرى كل القوائم |
| S4 | `passwordHash` و `twoFactorSecret` تُرجع في استجابات المستخدمين | `src/app/api/settings/users/route.ts:9-23, 77` | كسر كلمات المرور offline |
| S5 | كلمات مرور المنصات الحكومية (أبشر/مقيم/قوى) مخزنة نصًا وتُرجع في GET عام | `prisma/schema.prisma:1505`, `src/app/api/gov-platforms/route.ts:7-16` | استيلاء على حسابات الشركة الحكومية |
| S6 | Command Injection كـ root في لوحة إدارة المستأجرين (`name`/`domain` تُحقن في `rm -rf` و `DROP DATABASE` و nginx) | `radeef-manage/server.js:251-274, 292-399` | تنفيذ أوامر كـ root على السيرفر المستضيف لكل العملاء |
| S7 | كلمة مرور root SSH، IP السيرفر، وكلمة مرور Postgres مكتوبة في 20+ ملفًا | `deploy_*.js`, `diagnose*.js`, `radeef-manage.js`, `deploy_all.sh:33`, `radeef-manage/server.js` | تسريب المستودع = تسريب السيرفر كاملًا |
| S8 | رفع ملفات بلا مصادقة أو فحص نوع أو حجم إلى `public/uploads` | `src/app/api/upload/route.ts` | stored XSS، malware، امتلاء القرص |
| S9 | بوابة الموظف تقبل `userId` من query string وتُرجع أول موظف إن غاب | `src/app/api/portal/route.ts:9, 43-60` | IDOR على رواتب وIBAN كل الموظفين |
| S10 | `xlsx@0.18.5` مصابة بـ Prototype Pollution و ReDoS معروفتين، وتُستخدم في مسار استيراد غير مُصادَق | `package.json`, `src/app/api/employees/import/route.ts` | |

### 2.2 منطق الأعمال المالي

| # | الاكتشاف | الموقع | الأثر |
|---|---|---|---|
| B1 | أقساط السلف تُخصم لكن `remainingAmount` لا يُنقَص أبدًا | `src/app/api/payroll-hub/generate/route.ts:91-94`, `payroll-hub/route.ts:38-44` | الموظف يدفع القسط إلى الأبد |
| B2 | لا يمكن طلب إجازة ثانية بعد أول إجازة معتمدة (الحالة تبقى APPROVED) | `src/app/api/leaves/route.ts:34-43` | تعطل نظام الإجازات بعد أول استخدام |
| B3 | مبالغ نهاية الخدمة تُحسب في المتصفح ويثق بها السيرفر | `src/app/api/settlements/route.ts:61-70` | أي تعديل يدوي للطلب يصل للمحاسب |
| B4 | المكافآت غير الشهرية لا تُصرف أبدًا، وتُمسح عند تعديل الموظف | `generate/route.ts:67-69`, `employees/[id]/route.ts:96-108` | |
| B5 | توليد المسير يفشل بالكامل إن وُجد مسير معتمد سابق، وtransaction بحلقة تنتهي بـ timeout مع مئات الموظفين | `generate/route.ts:54-57, 150` | |
| B6 | مسار الأرشيف يرمي 500 دائمًا (قيمة `COMPLETED` غير موجودة في enum) | `src/app/api/incoming-requests/archive/route.ts:10` | |
| B7 | حالات Deduction/Loan/Leave متضاربة بين 4 مسارات اعتماد مختلفة بلا guards | `manager-portal`, `incoming-requests`, `dept-manager`, `leaves/[id]/action` | مخالفات معتمدة لا تُخصم، سلف لا تصل للمالية |
| B8 | رصيد الإجازة يُصفَّر عند العودة من الإجازة (`leaveAccrualStartDate = now`) | `leaves/[id]/action/route.ts:35-40` | ضياع الرصيد المتراكم |
| B9 | المال مخزن كـ `Float` في ~40 حقلًا | `prisma/schema.prisma` | أخطاء تقريب في المسيرات والتسويات |

### 2.3 البنية التحتية والبيانات

| # | الاكتشاف | الموقع | الأثر |
|---|---|---|---|
| I1 | لا migrations؛ `prisma db push --accept-data-loss` على dar فقط، وradeef/rakan لا يُطبَّق عليهما المخطط أصلًا | `deploy_all.sh:44` | فقدان بيانات صامت + schema drift بين المستأجرين |
| I2 | لا نسخ احتياطي دوري (pg_dump يعمل فقط عند تعليق الاشتراك، على نفس القرص) | `radeef-manage/server.js:563-568` | |
| I3 | التطبيق يعمل كـ root من `/root`، و`rsync /root/` ينسخ `.ssh` والنسخ الاحتياطية وuploads كل مستأجر إلى مجلدات المستأجرين الآخرين | `deploy_all.sh:15, 26` | تسريب وثائق بين العملاء |
| I4 | Prisma يسجّل كل استعلام SQL مع قيمه في الإنتاج، و10 مسارات تنشئ `new PrismaClient()` خاصًا | `src/lib/prisma.ts:8` وعشرة مسارات | PII في السجلات، استنزاف اتصالات Postgres |
| I5 | لا TLS في إعدادات nginx المولدة، لا health check، لا مراقبة، لا CI، لا اختبارات | كامل المشروع | |
| I6 | الملفات المرفوعة داخل `public/uploads` لا تُخدَّم بعد `next build` وتُحذف مع كل نشر | `src/app/api/upload/route.ts:18` | |
| I7 | لوحة المستأجرين تُدرج أي عملية PM2 تلقائيًا بانتهاء بعد شهر وتوقفها، وتعرض "الاشتراك غير نشط" عند أي 502 | `radeef-manage/server.js:195-217, 366` | تعليق العملاء الحاليين تلقائيًا |

---

## 3. الخطة المرحلية

التقدير بالأيام لمطور واحد متفرغ. المراحل 0 و1 لا يجوز نشر أي شيء جديد قبل إتمامها.

### المرحلة 0: إيقاف النزيف (يوم واحد، فورًا)

- [ ] تدوير كلمة مرور root SSH وكلمة مرور Postgres وأي SMTP، والانتقال إلى مفاتيح SSH مع تعطيل تسجيل دخول root بكلمة مرور.
- [ ] حذف 17 سكربت تشخيص/نشر من الجذر (`check_*.js`, `compare*.js`, `diagnose*.js`, `deploy_error/fix/radeef/all.js`, `find_radeef.js`, `rebuild.js`, `verify.js`, `test.js`). إذا كان المستودع على GitHub، تنظيف التاريخ بـ `git filter-repo`.
- [ ] إيقاف خدمة `radeef-manage` أو ربطها بـ `127.0.0.1` خلف VPN حتى إصلاح حقن الأوامر، وإزالة الافتراضي `admin/admin`.
- [ ] حذف الـ backdoor والمقارنة النصية من `login/route.ts`.
- [ ] تغيير `src/lib/prisma.ts` إلى `log: ['error']` في الإنتاج.
- [ ] أخذ `pg_dump -Fc` يدوي لقواعد المستأجرين الثلاثة وحفظه خارج السيرفر.
- [ ] تعطيل الإدراج التلقائي للمستأجرين في `radeef-manage/server.js:195-217` لتفادي تعليق العملاء الحاليين.

### المرحلة 1: المصادقة والتفويض (5 إلى 7 أيام)

- [ ] جلسات موقّعة: JWT بمكتبة `jose` في cookie `httpOnly; Secure; SameSite=Lax`، أو Auth.js. لا `userId` ولا `role` في localStorage.
- [ ] Helper مركزي `requireUser(req, roles?)` في `src/lib/auth.ts` يُستدعى في بداية كل من الـ 78 مسارًا، مع تحويل الفشل إلى 401/403.
- [ ] توسيع `middleware.ts` ليشمل `/api` باستثناء `auth/login` و `apply/*`.
- [ ] RBAC على السيرفر مصدره جدول `RolePermission`، وتوحيد `AppRole` مع enum Prisma (5 أدوار حقيقية تسقط حاليًا إلى EMPLOYEE).
- [ ] حذف زر "تجربة الأدوار" من `DashboardLayout.tsx` وtoggle HR/MANAGER في `penalties/page.tsx`.
- [ ] اشتقاق `employeeId` في مسارات `portal/*` من الجلسة فقط.
- [ ] `select` صريح يستبعد `passwordHash` و `twoFactorSecret` في كل استجابة (أو Prisma `omit`).
- [ ] تشفير `GovPlatform.password` بـ AES-256-GCM بمفتاح من env، وعدم إرجاعه في GET.
- [ ] رسالة خطأ موحدة في تسجيل الدخول + rate limiting (5 محاولات / 15 دقيقة).
- [ ] رفع الملفات: مصادقة + allowlist أنواع + حد 10MB + تخزين خارج `public/` (مجلد `UPLOAD_DIR` أو S3-compatible) مع مسار تحميل محمي.
- [ ] استبدال `xlsx` بـ `exceljs` أو تثبيتها من CDN الرسمي ≥0.20.
- [ ] Security headers في `next.config.ts` (HSTS, X-Frame-Options, nosniff, CSP أساسي).

### المرحلة 2: قاعدة البيانات والترحيل (3 إلى 4 أيام)

- [ ] إنشاء baseline migration من المخطط الحالي ثم `prisma migrate resolve --applied` على كل مستأجر بعد قياس الانحراف بـ `prisma migrate diff`.
- [ ] منع `db push` نهائيًا؛ الإنتاج يستخدم `prisma migrate deploy` مسبوقًا بـ `pg_dump`.
- [ ] Migration 1: إضافة `@@index` على كل أعمدة FK وأعمدة التصفية (الجدول الكامل في تقرير قاعدة البيانات، ~45 موديل).
- [ ] Migration 2: `updatedAt` للـ 22 موديل الناقص.
- [ ] Migration 3: تحويل حقول المال إلى `Decimal(12,2)` بدءًا بـ Payroll/Loan/Settlement/Deduction، مع serializer في الـ API.
- [ ] Migration 4: `onDelete: Restrict` على Loan/Deduction/Settlement/Investigation/AccidentClaim/Leave، وsoft-delete للشركة والفرع.
- [ ] Migration 5: تحويل `Loan.status`, `Deduction.status`, `Investigation.status`, `Asset.status` إلى enums بعد تنظيف القيم الموجودة.
- [ ] توحيد الاستيراد من `@/lib/prisma` في المسارات العشرة، وإضافة `connection_limit` في `DATABASE_URL`.
- [ ] `prisma/seed.ts` idempotent: الجنسيات، مصفوفة صلاحيات افتراضية، مدير أولي من env بكلمة مرور عشوائية.
- [ ] حذف `prisma.config.ts` (ميت في Prisma 5 ويستورد حزمًا غير مثبتة).
- [ ] توليد `employeeId` من SEQUENCE بدل `count()+1`.

### المرحلة 3: تصحيح منطق الأعمال المالي (5 إلى 6 أيام)

- [ ] `src/lib/payroll.ts`: توليد المسير بـ `createMany`، timeout مناسب، التحقق من الشهر/السنة، 409 عند وجود مسير معتمد، proration للمعيّن/المُنهى داخل الشهر، معدل الأوفرتايم و GOSI من `SystemSetting`.
- [ ] إنقاص `Loan.remainingAmount` داخل transaction عند اعتماد المسير + جدول `LoanInstallment`.
- [ ] `src/lib/settlement.ts`: إعادة حساب نهاية الخدمة server-side من `joinDate/lastWorkingDate/basicSalary` (المعادلة الصحيحة موجودة في `settlements/new/page.tsx:327-356`).
- [ ] `src/lib/leave.ts`: معادلة رصيد واحدة + جدول `LeaveBalance` بدل تصفير `leaveAccrualStartDate`، وحالة `COMPLETED` للإجازة المنتهية، وإصلاح شرط "إجازة قائمة".
- [ ] State machine واحد لكل من Deduction/Loan/Leave/Settlement/Transfer مع guard على الحالة السابقة، وتوحيد مسارات الاعتماد الأربعة في دالة واحدة داخل `$transaction`.
- [ ] تضمين المكافآت غير الشهرية في المسير ووسمها بعد الصرف، وإيقاف `deleteMany` للبدلات في PUT الموظف.
- [ ] إصلاح `incoming-requests/archive` و `notifications` (`leave.days` → `totalDays`).
- [ ] `src/lib/dates.ts`: دالة "اليوم" واحدة بتوقيت الرياض، و`differenceInDays` واحدة، وتوحيد عتبات التنبيهات الخمس من `SystemSetting`.

### المرحلة 4: جودة الـ API (4 إلى 5 أيام)

- [ ] `zod` لكل POST/PUT/PATCH، مع بناء `data` من الحقول الموجودة فقط (PUT الموظف يصفّر الراتب حاليًا عند غياب الحقل).
- [ ] `src/lib/http.ts`: استجابة خطأ موحدة، 400/404/409 بدل 500، عدم إرجاع `err.message` الخام، تحويل P2002/P2025.
- [ ] Pagination (`take/skip` + `select` محدود) لمسارات القوائم، خاصة `employees`, `payroll-hub`, `search`, `owner-reports`.
- [ ] `Promise.all` في `dashboard`, `renewals`, `incoming-requests`, `hr/alerts`, `owner-reports`.
- [ ] Audit log عبر Prisma `$extends` لكل update/delete على الموديلات الحساسة مع `userId` و IP، وتسجيل LOGIN.
- [ ] البريد: transporter واحد، timeout، إرسال خارج الطلب (queue بسيط في DB + cron).
- [ ] إزالة `ignoreBuildErrors` وإصلاح الخطأين (أنواع nodemailer، وحقل `taxCertificateUrl` غير الموجود في موديل Company).

### المرحلة 5: الواجهة الأمامية (5 إلى 7 أيام)

- [ ] نقل `DashboardLayout` إلى `src/app/(dashboard)/layout.tsx` بدل استيراده في 85 صفحة (يوقف 3 طلبات شبكة في كل تنقّل).
- [ ] مصدر واحد للقوائم `src/lib/menu.ts` بمفاتيح إنجليزية، تستخدمه صفحة الصلاحيات والـ sidebar (7 من 14 قسمًا لا تتطابق عناوينها حاليًا).
- [ ] `error.tsx` + `global-error.tsx` + `not-found.tsx` + `loading.tsx` عامة بالعربية، وإزالة طباعة stack trace في `renewals/error.tsx`.
- [ ] طبقة `useApi`/SWR واحدة: loading، error ظاهر للمستخدم، abort، cache.
- [ ] إصلاح `SearchableSelect`: `required` عبر hidden input لا يعمل في المتصفحات، إضافة keyboard navigation و ARIA.
- [ ] `disabled={isSubmitting}` في 17 نموذجًا بلا حماية، و`beforeunload` للنماذج الطويلة.
- [ ] `Toast` + `ConfirmDialog` بدل 205 `alert` و 35 `confirm`.
- [ ] استبدال 37 Tailwind class ديناميكي (`bg-${color}-50`) بخرائط ثابتة، وإضافة أوزان الخط 800/900 لـ Cairo.
- [ ] قرار تقويم صريح (`ar-SA-u-ca-gregory-nu-latn` على الأرجح) في `formatDate` واحد بدل 95 استخدامًا هجريًا مختلطًا.
- [ ] `dynamic import` لـ xlsx، `next/image`، `useMemo` لجداول الموظفين.
- [ ] حذف زرع الجنسيات عند فتح نموذج موظف جديد (`employees/new/page.tsx:118-131`).

### المرحلة 6: النشر والتشغيل (4 إلى 5 أيام)

- [ ] الانتقال إلى Docker Compose: صورة واحدة بوسم git SHA، حاوية لكل مستأجر، `env_file` خارج المستودع بصلاحية 600، volume للـ uploads لكل مستأجر، `healthcheck` على `/api/health`.
- [ ] مستخدم نظام `radeef` غير root، مسارات `/opt/radeef` و `/var/lib/radeef/<tenant>`.
- [ ] دور Postgres منفصل لكل مستأجر بدل superuser مشترك.
- [ ] Nginx: قالب واحد بـ TLS (wildcard عبر DNS-01) + HSTS + `client_max_body_size`، وصفحة صيانة عامة لـ 502 بدل صفحة "الاشتراك غير نشط".
- [ ] نسخ احتياطي يومي: `pg_dump -Fc` + `tar` للـ uploads → تخزين خارجي (B2/Hetzner) مع retention 7/4/6، واختبار استعادة شهري.
- [ ] GitHub Actions: `npm ci` → `prisma generate` → `tsc --noEmit` → `eslint` → `next build` على كل PR، وdeploy عند tag عبر SSH key.
- [ ] Zero-downtime: pull الصورة → backup → `migrate deploy` → حاوية جديدة على منفذ بديل → healthcheck → تبديل upstream في nginx → إيقاف القديمة. Rollback = تبديل المنفذ.
- [ ] Sentry أو ما يعادله مع tag للمستأجر، pino JSON logs، UptimeRobot على كل نطاق.
- [ ] `.env.example` كامل، `postinstall: prisma generate`, `engines`, `output: 'standalone'`.
- [ ] إعادة كتابة `radeef-manage` بـ whitelist regex على `name`/`domain`، `execFile` بدل تركيب نصوص، جلسات موقّعة، CORS محدد.
- [ ] نقل ملفات العروض التجارية (~1MB) وقوالب Excel خارج المستودع، وتصحيح README (Prisma 5 لا 7، 80 مسار لا 44، لا `tailwind.config.ts`).

### المرحلة 7: الاختبارات والاعتماد (3 إلى 4 أيام)

- [ ] Vitest لدوال `src/lib/{payroll,settlement,leave,dates}.ts` مع حالات نظام العمل السعودي (نهاية الخدمة قبل/بعد 5 سنوات، الاستقالة بنسبها، الإجازة المرضية 30/60/90).
- [ ] اختبارات تكامل للمسارات الحرجة: login، employees، payroll generate، leaves action، settlements.
- [ ] اختبار E2E واحد على الأقل (Playwright) لمسار: دخول → إضافة موظف → طلب إجازة → اعتماد → توليد مسير.
- [ ] فحص اختراق خفيف بعد المرحلة 1 (يمكن بأدوات آلية مثل ZAP).
- [ ] تخفيض `any` تدريجيًا (982 حاليًا) بتفعيل القاعدة كـ error على الملفات الجديدة فقط.

---

## 4. الجدول الزمني الإجمالي

| المرحلة | المدة التقديرية | يمكن التوازي مع |
|---|---|---|
| 0. إيقاف النزيف | 1 يوم | لا شيء، تُنفذ أولًا |
| 1. المصادقة والتفويض | 5 إلى 7 أيام | المرحلة 2 |
| 2. قاعدة البيانات | 3 إلى 4 أيام | المرحلة 1 |
| 3. منطق الأعمال | 5 إلى 6 أيام | المرحلة 5 |
| 4. جودة الـ API | 4 إلى 5 أيام | المرحلة 5 |
| 5. الواجهة | 5 إلى 7 أيام | المراحل 3 و4 |
| 6. النشر والتشغيل | 4 إلى 5 أيام | المرحلة 5 |
| 7. الاختبارات | 3 إلى 4 أيام | بعد 3 و4 |
| **الإجمالي** | **30 إلى 39 يوم عمل لمطور واحد، أو 4 إلى 5 أسابيع لمطورين اثنين** | |

**الحد الأدنى للنشر الآمن (MVP-Secure):** المراحل 0 + 1 + 2 + أول 5 بنود من المرحلة 3 + النسخ الاحتياطي والـ TLS من المرحلة 6. حوالي 15 يوم عمل.

---

## 5. معايير القبول للإنتاج (Definition of Done)

- [ ] 0 مسار API يمكن الوصول إليه بلا جلسة صالحة (باستثناء login و apply).
- [ ] 0 سر في المستودع (فحص بـ gitleaks في CI).
- [ ] `tsc --noEmit` و `eslint` يمران في CI بلا `ignoreBuildErrors`.
- [ ] كل تغيير مخطط عبر migration مراجَع، و`db push` محظور.
- [ ] نسخة احتياطية يومية مُختبرة الاستعادة خلال آخر 30 يومًا.
- [ ] اختبارات وحدة تمر لحسابات المسير ونهاية الخدمة ورصيد الإجازة.
- [ ] TLS على كل نطاق مع درجة A في SSL Labs.
- [ ] Health check ومراقبة uptime وتتبع أخطاء فعّالة لكل مستأجر.
- [ ] الأدوار الحقيقية الأحد عشر كلها ترى القوائم الصحيحة من السيرفر لا من localStorage.
- [ ] الملفات المرفوعة تُخدَّم عبر مسار محمي وتُنسخ احتياطيًا.
