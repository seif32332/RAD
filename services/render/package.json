{
  "name": "radeef-render",
  "version": "1.0.0",
  "private": true,
  "description": "Internal document renderer (Typst) for Radeef: one instance per host, 127.0.0.1 only, stateless.",
  "type": "module",
  "engines": {
    "node": ">=20.9"
  },
  "scripts": {
    "start": "node src/main.mjs",
    "test": "node --test --test-concurrency=1 test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json"
  }
}
