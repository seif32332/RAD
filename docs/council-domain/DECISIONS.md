# قرارات مجلس خبراء المجالات

## DOM-001 — حوكمة التنفيذ

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
تُدمج بنود MUST في اللجان الخمس (نحو 60 بنداً مكرراً) في موجة 0 واحدة من 8 حزم عمل بلا أي ترحيل للمخطط، هدفها إيقاف الضرر القائم. كل بند مكرر يُنفَّذ تحت معرّف واحد. الآيبان: HR-05 وUX-01 وUX-05 وLEGAL-09. الإشعار الكاذب: HR-02 وUX-02 وLEGAL-01. العهد: HR-10 وLOGISTICS-01. سجل التغييرات: DATA-05 وHR-24 وLEGAL-09 وLEGAL-23.

### السبب
سقف MUST عند 60% من السعة المقاسة في DEC-002 (شرط حاكم). شرط DEC-004 المانع لم يتحقق: EXECUTION_LOG.md:40 يقول إن ترحيلات CI لم تُشغَّل على GitHub. الفريقان الأحمران اتفقا على هذا الترتيب.

### الأدلة
- docs/council/DECISIONS.md:129-160 (DEC-002 شرط حاكم)
- docs/council/EXECUTION_LOG.md:40
- docs/council/DECISIONS.md:335 (DEC-004 شرط مانع)

### الفريق الأحمر
الفريق الأول: 60 بنداً تتجاوز السقف بأضعاف. الفريق الثاني: نحو 25 بنداً تغيّر المخطط فوق خط نشر لم يُختبر.

### طريقة التحقق
كل حزمة تُغلق باختباراتها، و559 اختباراً قائماً تبقى ناجحة، ولا يظهر أي ملف جديد في prisma/migrations خلال موجة 0.

---

## DOM-002 — الشؤون القانونية والتأديب

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
تُحذف فوراً إضافة «[تم إرسال إشعار للموظف...]» ويصبح الخيار الافتراضي false. لا يُكتب «تم الإرسال» إلا إذا حدث إرسال فعلي، وSMTP معلق في DEC-009. تُقفل findings وfinalDecision بعد صدور الحكم، ويُمنع COMPLETED_GUILTY دون نتائج مكتوبة. لا تُعدَّل الملفات التاريخية آلياً؛ يُسلَّم لكل مستأجر تقرير قراءة فقط، ويقرر العميل مع مستشاره.

### السبب
سجل يدّعي إبلاغاً لم يحدث يطعن في مصداقية ملف المنشأة كله (المادة 71). تحقق منه رئيس اللجنة في الكود وعلى البيانات: 12 من 18. تعديل الملفات القائمة إقرار مكتوب قد يُستخدم ضد العميل.

### الأدلة
- src/app/api/legal/investigations/route.ts:223-226
- src/app/legal/investigations/page.tsx:69
- src/app/api/legal/investigations/route.ts:36-41

### الفريق الأحمر
الفريقان: تعديل السجلات الحية عبث بالأدلة.

### الخلافات
HR-02 طلب حذف النص من المحاضر، وLEGAL-01 طلب ملاحظة تصحيحية. رُفض الاثنان لصالح نهج الفريق الأحمر: لا يُعدَّل أي سجل تاريخي آلياً.

### طريقة التحقق
اختبار وحدة: buildCreateNotes لا تُرجع العبارة أبداً. اختبار HTTP: PUT يغيّر findings بعد COMPLETED_GUILTY يُرد عليه بـ409.

---

## DOM-003 — الرواتب والتأديب

**يحتاج موافقة المالك:** نعم · **الثقة:** HIGH

### القرار
سقوف الحسم على مرحلتين. المرحلة 1 (الآن، بلا تغيير في أي رقم): needsReview مع سبب مرمّز في reviewNote إذا تجاوزت الغرامات الشهرية أجر 5 أيام (م70)، أو قسط السلفة 10% (م92/1)، أو مجموع الحسومات نصف الأجر المستحق بعد الغياب (م93)، أو كان الصافي ≤ 0. ويُضاف تنبيه جاهزية الصرف الذي قرره DEC-006(2) ولم يُنفَّذ: آيبان مفقود أو غير صالح، وصرف نقدي، وأساسي صفر. المرحلة 2: الفرض والترحيل للشهر التالي عبر Deduction.remainingAmount، بعد تشغيل ظل وموافقة المالك والمستشار. ويُخفض سقف deductionDays وpenaltyDays في الـAPI إلى 5 أيام الآن.

### السبب
النص النظامي متحقق منه (laws.boe.gov.sa). لكن الترحيل يحتاج حالة غير موجودة في المخطط ويغيّر جداول السلف. والغياب ليس حسماً فلا يدخل السقف. وأرقام المادتين صُححت: حد النصف في المادة 93، ودين الحكم بالربع في 92/6.

### الأدلة
- src/lib/payroll-core.ts:604-622,619
- src/app/api/payroll-hub/route.ts:225,444
- src/app/api/legal/investigations/route.ts:83
- docs/council/DECISIONS.md (DEC-006 القرار 2)

### الفريق الأحمر
الفريق 1: الغياب ليس حسماً، ومصادر المواد خاطئة. الفريق 2: الترحيل جهده L، والمرحلة الأولى S بلا ترحيل.

### الخلافات
خبير الرواتب يريد الترحيل الآن، وخبير النظام يترك الخيار للمالك بين المنع والترحيل. المجلس اختار تنبيهاً الآن والترحيل بعد القرار.

### طريقة التحقق
اختبار وحدة للسيناريو (إجمالي 5,000، جزاءات 2,700، قسط 3,000): الأرقام لا تتغير، وneedsReview=true مع رموز CAP_PENALTY وCAP_LOAN وNET_ZERO. POST CREATE_DEDUCTION بعدد أيام 6 يُرد عليه بـ400.

---

## DOM-004 — الإنهاء والتصفية

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
يُسمح بإنشاء تصفية END_OF_SERVICE لموظف مُنهى ليست له تصفية END_OF_SERVICE غير مرفوضة. يُمنع ABSCOND قبل انتهاء الإجازة، وأثناء الإجازة الوضعية أو المرضية. ويُمنع إنهاء الخدمة من الملف أثناء إجازة وضع أو مرضية سارية، إلا لـSUPER_ADMIN أو LEGAL_ADMIN مع سبب مكتوب. PROBATION يُقبل فقط إذا لم يتجاوز آخر يوم عمل probationEndDate، أو joinDate + 180 يوماً عند غيابه. ARTICLE_80 يشترط رقم الفقرة (1-9) ووجود تحقيق مُدان للموظف، وإلا يُرفض برسالة عربية تذكر المبلغ المُسقط. زر «إنهاء الخدمات» يتحول إلى فتح معالج التصفية.

### السبب
الإنهاء المباشر يحرم الموظف من المستحقات داخل النظام، وهذا يمس مهلة المادة 88. خبير النظام أنهى حياً خدمة موظفة في إجازة وضع. والتصفير بلا شرط أسقط 17,722 ريالاً في معاينة حية.

### الأدلة
- src/app/api/settlements/route.ts:267,275
- src/lib/settlement.ts:122
- src/app/api/leaves/[id]/action/route.ts:124-133
- src/app/api/employees/[id]/route.ts:302-310

### الفريق الأحمر
الفريق 2: حاجز الـ15 يوم غياب يعتمد على سجلات ABSENT لا يولّدها النظام. لذلك يُحتسب الحاجز من تاريخ نهاية الإجازة، لا من عدد سجلات الحضور.

### الخلافات
خبير العمليات أراد إلغاء الزر، وخبير النظام أراد إبقاءه بحواجز. الحل: الزر يفتح المعالج، والحاجز في الخادم.

### طريقة التحقق
اختبارات HTTP: (1) موظف مُنهى بلا تصفية يُنشأ له POST settlement ويُرد بـ201. (2) ABSCOND أثناء إجازة MATERNITY يُرد بـ409. (3) PROBATION لموظف خدمته 5 سنوات يُرد بـ400. (4) ARTICLE_80 بلا تحقيق مُدان يُرد بـ400.

---

## DOM-005 — العهد وإخلاء الطرف

**يحتاج موافقة المالك:** نعم · **الثقة:** MEDIUM

### القرار
لا يُمنع إنشاء التصفية ولا صرفها بسبب العهد. تُضاف على الخادم تحذيرات في معاينة التصفية تغطي Asset وTelecomSim وVehicle.driverId، والإجازات المستقبلية، وتأشيرات الخروج والعودة المفتوحة، وطلبات تجديد الوثائق المعلقة. تبقى حالة المنع الحالية في الواجهة كما هي حتى يقرر المالك، ويُصلح خلل allowAssetsRetention. ويُرفض إسناد أي عهدة أو مركبة أو شريحة لموظف منتهية خدمته. القرار بين المنع والحسم كدين عمل والاستثناء متروك للمالك والمستشار.

### السبب
المادة 88 توجب التصفية خلال أسبوع أو أسبوعين، وتجيز حسم دين العمل. فالمنع على الخادم (LOGISTICS-01) قد يحتجز المستحقات بلا سند. وخبير العهد أثبت حياً أن الخادم لا يفحص العهد أصلاً.

### الأدلة
- src/app/settlements/new/page.tsx:436-445,696,1098
- src/lib/finance.ts:586-597
- src/app/api/services/_lib.ts:18-38

### الفريق الأحمر
الفريق 1 والفريق 2 (HR-03 مقابل LOGISTICS-01): التوصيتان معاً تصنعان طريقاً مسدوداً.

### الخلافات
لجنة اللوجستيات تريد رفضاً بـ409. الفريق الأحمر 1 يرى ذلك مخالفة للمادة 88. رُجّح التحذير لأن الدليل النظامي أقوى، ويبقى الخلاف مفتوحاً للمستشار.

### طريقة التحقق
معاينة تصفية لموظف عنده لابتوب وشريحة ومركبة ترجع warnings فيها 3 بنود. PATCH assign لموظف isTerminated يُرد عليه بـ409.

---

## DOM-006 — الخدمة الذاتية وسلامة البيانات

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
يُحذف استخراج الجوال والبريد والآيبان بالتعبيرات النمطية من النص الحر. البوابة ترسل حقولاً موسومة، والاعتماد يطبّق الجوال والبريد فقط إذا جاءا في سطر موسوم وصالح. الآيبان لا يُطبَّق آلياً أبداً؛ يعدّله HR يدوياً في الملف بعد التحقق من شهادة الآيبان. طلبات الخطابات وتحديث البيانات لا تمر بالمدير المباشر. أما المعتمِد النهائي لتغيير الآيبان (HR أو الرواتب أو المالية، ومعتمد ثانٍ أم لا) فقرار للمالك.

### السبب
الخبير أثبت حياً أن الجوال استُبدل بـ4420000001، وتحقق رئيس اللجنة من السطر 877. الإيقاف لا يحتاج قراراً لأنه يزيل فعلاً آلياً خاطئاً. أما من يعتمد فهو حوكمة.

### الأدلة
- src/lib/hr-workflows.ts:875-887
- src/app/portal/page.tsx:657-676
- src/app/api/dept-manager/route.ts:119-127

### الفريق الأحمر
الفريق 2: أربع توصيات تقترح ثلاثة معتمدين مختلفين. يلزم قرار واحد من المالك.

### الخلافات
UX-01 يرى الـchecksum مانعاً للحفظ، وDEC-006 يرى التحذير فقط. في موجة 0 لا يُطبَّق الآيبان آلياً، فلا حاجة لحسم هذا الخلاف الآن.

### طريقة التحقق
اختبار وحدة: نص فيه آيبان فقط يُرجع updatedFields=[] ولا يغيّر mobileNumber، ورسالة النجاح تذكر الحقول المطبقة فعلاً.

---

## DOM-007 — تقييم الأداء

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
يُزال «المساعد الذكي» واقتراح درجات الانضباط كلياً، ولا يُكتفى بإعادة تسميته. يرى المقيّم رابطاً إلى سجل الحضور فقط.

### السبب
الخبير أثبت حياً أن إجازة أمومة مستقبلية خفّضت الدرجة إلى 2 من 5. وهذا يعارض الحظر الدائم في DEC-006 على التقييمات والتوصيات المولدة، وفيه خطر تمييز (المادتان 3 و61). والحضور يدوي وغير موثوق (DATA-16).

### الأدلة
- src/app/api/evaluations/scoring.ts:141-160
- src/app/api/evaluations/route.ts:268-310
- docs/council/DECISIONS.md (DEC-006 القرار 5)

### الفريق الأحمر
الفريق 1: الإبقاء يعيد الخطر ذاته مع أي خلل في الحضور.

### الخلافات
رئيس لجنة الموارد البشرية اقترح إعادة التسمية إلى «مؤشرات الحضور». رُجّح رأي الفريق الأحمر بالإزالة.

### طريقة التحقق
GET view=smart-suggest يُرد عليه بـ404 أو 410، والصفحة لا تحتوي كلمة «ذكي».

---

## DOM-008 — البيانات الرئيسية

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
الجنس إلزامي بلا قيمة افتراضية في الإنشاء، وفي صفوف الاستيراد الجديدة. عند تحديث صف قائم بلا جنس تُبقى القيمة الحالية ويظهر تحذير. لا يُدّعى وجود تقرير تاريخي بالقيم الافتراضية، لأن مصدر القيمة غير محفوظ. يُطلب من HR تأكيد جنس من لها أو قدّمت طلب إجازة وضع.

### السبب
zGenderOrDefault يحوّل القيمة الفارغة إلى MALE، وهذا يحجب إجازة الوضع. الإصلاح يطبّق مبدأ DEC-002 نفسه في الجنسية.

### الأدلة
- src/lib/employee.ts:508-511
- src/app/api/employees/route.ts:61
- src/app/api/employees/import/route.ts:463-464

### الفريق الأحمر
الفريق 2: التقرير التاريخي مستحيل، والإلزام في التحديث يكسر قوالب Excel الشهرية.

### طريقة التحقق
POST /api/employees بلا gender يُرد عليه بـ400 برسالة عربية. استيراد صف جديد بلا جنس يُسجَّل خطأً للصف، والصف المحدَّث يُسجَّل تحذيراً.

---

## DOM-009 — التقارير والمؤشرات

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
تُصحَّح الأرقام المضللة الحالية دون بناء طبقة تحليلات. (1) «المسير المرجعي» أحدث شهر APPROVED أو PAID لا يتجاوز الشهر الحالي بتوقيت الرياض. (2) تُستبعد فئة LEGAL من adminAlerts في إجمالي اللوحة. (3) تقرير المالك يفصل «فعلي» من أسطر المسير المخزنة عن «تقديري» للمستقبل، ويعرض «N بند بلا تكلفة مسجلة» بدل 0، ويحسب الإيجار حسب دورية الدفع، ويصحح أعمدة القضايا والعقود ويترجم الحالات. لا يُقيَّد zYear في موجة 0. يؤجَّل DATA-09 إلى SHOULD بخمسة أرقام، دون مؤشر سعودة.

### السبب
تحقق حي: 2098-3 و78 مقابل 72. تقييد zYear يكسر بيانات الاختبار التي يقوم عليها التحقق الهندسي. ولا يوجد أي طلب موثق من عميل لحزمة مؤشرات.

### الأدلة
- src/app/api/dashboard/route.ts:55-58,186-205
- src/app/api/owner-reports/route.ts:97-100,128-135
- src/app/owner-reports/page.tsx:179-206

### الفريق الأحمر
الفريق 2: gosiEmployer=0 في كل الأسطر السابقة للترحيل 5، فتكلفة صاحب العمل التاريخية ناقصة ويجب وسمها.

### الخلافات
لجنة البيانات صنّفت DATA-04 وDATA-09 MUST. الفريق الأحمر خفّضهما، والمجلس معه.

### طريقة التحقق
اختبار: مسودة لشهر في 2098 لا تظهر في latestPayrollMonth. مجموع اللوحة يساوي عدد عناصر المخاطر المميزة في الشاشة المجمعة. caseNumber لا يساوي subject.

---

## DOM-010 — الامتثال التنظيمي للمنشأة

**يحتاج موافقة المالك:** لا · **الثقة:** MEDIUM

### القرار
يُعاد وسم تنبيهات السجل التجاري وطلبات سدادها إلى «موعد التأكيد السنوي للسجل التجاري»، وتُحذف عبارة «انتهى السجل». يبقى الحقل المخزّن كما هو في موجة 0، بلا ترحيل.

### السبب
نظام السجل التجاري الجديد ألغى تاريخ الانتهاء واعتمد التأكيد السنوي (mc.gov.sa، فتحها خبير الحوكمة والفريق الأحمر 1). فالتنبيه الحالي معلومة نظامية خاطئة تظهر للمالك اليوم، وليس مجرد تحسين يُصنَّف SHOULD.

### الأدلة
- src/lib/alerts.ts:490-491
- src/app/api/renewals/route.ts:276
- src/app/api/renewals/action/route.ts:49

### الفريق الأحمر
الفريق 1: لم تصنّفه أي لجنة MUST.

### الخلافات
اللجنة القانونية صنّفته SHOULD (LEGAL-25)، والفريق الأحمر رقّاه إلى MUST، والمجلس اعتمد MUST لأن جهده S.

### طريقة التحقق
لا تحتوي أي رسالة تنبيه في alerts.ts أو jobs.mjs أو renewals على عبارة «انتهى السجل التجاري» أو «ينتهي السجل التجاري».

---

## DOM-011 — التنبيهات والتجديدات

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
تصل التواريخ التي يخزنها النظام إلى أصحابها. تُضاف وثائق المركبة الست إلى مركز التجديدات وإجراء التجديد. تُضاف عقود النفايات والسلامة والكاميرات إلى تنبيهات المالك والبريد اليومي. يُرفض التاريخ الجديد إذا كان اليوم أو قبله إلا بتأكيد صريح. يصبح عنوان طلب السداد عربياً ويذكر اسم الكيان. تُعرض الوكالات والعقود القانونية في طابور GOV للقراءة فقط. ويُحذف نص «مُحال للتنفيذ» الآلي من تنبيه السند، ويُكتب النص حسب صفة الشركة: دائنة أو مدينة.

### السبب
الإصلاحات كلها بجهد S، وتجعل المنتج يفعل ما يدّعيه. تحقق منها رئيس لجنة اللوجستيات ورئيس اللجنة القانونية في الكود. والفريق الأحمر أبقى عليها كما هي.

### الأدلة
- src/app/api/renewals/route.ts:141,294-295
- src/app/api/renewals/action/route.ts:79-94,128-145,180,238-240
- src/lib/alerts.ts:123-125,175,498-503,646-670

### الفريق الأحمر
الفريق 2: LEGAL-08 يسحب عملاً يقوم به HR_MANAGER ضمن مجموعة GOV. العرض للقراءة مع رابط يحافظ على الرؤية.

### طريقة التحقق
اختبار: فرع عقد سلامته منتهٍ يظهر في /api/admin/alerts. مركبة فحصها منتهٍ تظهر في /api/renewals. POST action لوكالة من gov-relations يُرد عليه بـ403.

---

## DOM-012 — الرواتب

**يحتاج موافقة المالك:** نعم · **الثقة:** MEDIUM

### القرار
أساس العمل الإضافي (الأجر الفعلي أم الأساسي) يصبح إعداداً لكل مستأجر. القيمة الافتراضية تبقى على السلوك الحالي حتى يصدر رأي المستشار المكلف في DEC-003. يُضاف خيار التعويض بإجازة مدفوعة باتفاق (المادة 107 المعدلة). ويُعرض تقرير فروق للأشهر المعتمدة، دون تعديلها.

### السبب
تفسير «أجر الساعة» حكم ممارس لا نص صريح، وتغييره يغيّر الصافي الجاري دون إخطار. أما خيار الإجازة التعويضية فنصه متحقق منه لدى الفريق الأحمر.

### الأدلة
- src/lib/payroll-core.ts:142-146,190

### الفريق الأحمر
الفريق 1 (خيار الإجازة التعويضية) والفريق 2 (تشغيل الظل).

### الخلافات
رئيس لجنة الموارد البشرية صنّفه MUST مع جعل الأجر الفعلي افتراضياً. الفريقان الأحمران رفضا تغيير الافتراضي قبل المستشار.

### طريقة التحقق
تشغيل ظل على آخر 3 أشهر لكل مستأجر، مع تقرير فروق يوقّعه المالك قبل تغيير الإعداد.

---

## DOM-013 — فصل الصلاحيات

**يحتاج موافقة المالك:** نعم · **الثقة:** MEDIUM

### القرار
يُعمَّم decideMakerChecker الموجود ليشمل اعتماد المسير وتسجيل صرفه وتغيير الآيبان والبنك وشطب العهد، بدل بناء آليات منفصلة. يكون الفصل الكامل هو الافتراضي، والتجاوز للمنشأة ذات المستخدم المالي الواحد مع إشعار فوري للمالك. إشعار الموظف بتغيير الآيبان MUST بلا استثناء.

### السبب
شخص واحد يعتمد مسيراً بأكثر من مليون ويسجّل صرفه. وفرض الفصل إلزامياً قد يوقف مسير عميل صغير.

### الأدلة
- src/lib/constants.ts:52-54
- src/app/api/payroll-hub/route.ts:372-389
- src/app/api/payments/maker-checker.ts:15-35

### الفريق الأحمر
الفريق 2: allow_self_approval يعطّل كل الضوابط بإعداد واحد، وHR-11 يفترض جدول PayrollRun غير موجود، فجهده M لا S.

### طريقة التحقق
اختبار HTTP: المستخدم الذي ولّد المسير يُرد عليه بـ403 عند الاعتماد عندما يكون الإعداد على الافتراضي.

---

## DOM-014 — هندسة المخطط

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
كل التغييرات على المخطط تُجمع في دفعة expand واحدة إضافية فقط، ولا تُنفَّذ قبل استيفاء شروط DEC-004 (1) إلى (3): تشغيل ترحيلات CI، وmigrate status/diff على القواعد الحية، وخطة تراجع. ما يدخل الدفعة، وكل عمود فيها nullable: لقطة الفاعل في AuditLog (actorEmail وactorName وactorRole). جدول EmployeeHistory واحد، والآيبان فيه مقنّع. لقطة سطر المسير (legalCompanyId وbranchId وdepartmentId وibanLast4 وbankCode وpaymentMethod وsnapshotAt)، بلا ملء رجعي للآيبان. في Deduction: discoveredAt وremainingAmount. في Investigation: chargeText وchargeNotifiedAt وnotificationMethod وemployeeStatement وstatementRefused وhearingAt. في Asset: purchaseCost وserialNumber. في AccidentClaim: driverId. في Employee: genderSource وnationalityId.

### السبب
الشرط المانع لم يتحقق (EXECUTION_LOG.md:40). والملء الرجعي للآيبان في أسطر PAID يصنع سجلاً مالياً كاذباً.

### الأدلة
- docs/council/DECISIONS.md:326-345
- docs/council/EXECUTION_LOG.md:40
- prisma/schema.prisma:80,655-697,843-892

### الفريق الأحمر
الفريق 2: ثلاثة تصاميم متداخلة لسجل التغييرات، وlogAudit يبتلع الأخطاء ويقص النص عند 10,000 حرف ولا يحجب الآيبان.

### الخلافات
لجنة البيانات قدّرت اللقطة بجهد S، ولجنة الموارد البشرية بجهد L. قدّرها المجلس بجهد M: الأعمدة دون تشغيل لكل منشأة.

### طريقة التحقق
migrate diff على نسخ القواعد الثلاث يعطي صفر انحراف قبل الدفعة، وكل الأعمدة الجديدة nullable.

---

## DOM-015 — مبدأ التكامل مع المنصات الحكومية

**يحتاج موافقة المالك:** لا · **الثقة:** HIGH

### القرار
رديف يحفظ المرجع الرسمي فقط: الرقم، والرابط، وتاريخ المتابعة التالي. لا يعيد بناء حالة تديرها منصة حكومية. تبعاً لذلك: لا حالات تنفيذ للسندات (نافذ وناجز)، ولا جدول جلسات بل حقل «الجلسة القادمة»، ولا لوحة نسبة توثيق قوى بل حقل لكل موظف وقائمة بغير الموثقين، ولا ملف حماية أجور لكل منشأة قبل قالب حقيقي من مدد أو البنك، ولا سجل مخالفات مرورية ولا خصمها من الراتب.

### السبب
إعادة البناء تنتج نسخة ثانية تتقادم عن المصدر الرسمي. والمخالفة المرورية ليست من حالات الحسم في المادة 92، فخصمها بلا موافقة خطية غير نظامي.

### الأدلة
- docs/council/DECISIONS.md (DEC-003 القرار 8 و9)

### الفريق الأحمر
الفريق 1: أغلب بنود DO_NOT في اللجان تتناقض مع بنود MUST لديها.

### الخلافات
اللجنة القانونية (LEGAL-14 وLEGAL-16 وLEGAL-26) ولجنة اللوجستيات (LOGISTICS-18) اقترحتا بناء هذه الحالات. رفضها المجلس.

### طريقة التحقق
مراجعة كل بند SHOULD جديد على هذه القاعدة قبل دخوله الخطة.

---

## DOM-016 — قياس الاستخدام ومعالجة التاريخ

**يحتاج موافقة المالك:** نعم · **الثقة:** HIGH

### القرار
قبل أي عمل بجهد M أو L في الوحدات القانونية واللوجستية والتحليلية والتوظيف والتقييم، يُشغَّل على كل مستأجر حي استعلام عدّ قراءة فقط: عدد الصفوف وآخر تاريخ إنشاء لكل جدول. ويُسلَّم للمالك تقرير قراءة فقط بالسجلات المتضررة تاريخياً: تحقيقات فيها الإشعار الكاذب، وتصفيات صُفّرت مكافأتها وسببها PROBATION أو ARTICLE_80، وموظفون isTerminated بلا تصفية END_OF_SERVICE، وموظفات مسجلات MALE ولهن طلب وضع، وأرقام جوال تغيّرت بعد اعتماد «تحديث بياناتي»، وحسابات إدارية غير مرتبطة بموظف.

### السبب
كل أرقام المجالس محسوبة على بيانات اختبار بتواريخ بين 2030 و2303. حجم المشكلة الفعلي مجهول، ولا توجد خطة للسجلات الضارة القائمة.

### الأدلة
- docs/council/EXECUTIVE_BRIEF.md (سقف السعة)
- بيانات الاختبار: scratchpad/council2/redteam-impl/emps.json

### الفريق الأحمر
الفريقان: لم تقس أي لجنة الاستخدام، ولا يوجد إجراء لمعالجة السجلات الحية.

### طريقة التحقق
تقرير لكل مستأجر، موقّع من المالك، يحدد الوحدات التي تستحق عمقاً والسجلات التي تُعرض على مستشار العميل.

---

## DOM-017 — التسويق والمسؤولية

**يحتاج موافقة المالك:** نعم · **الثقة:** HIGH

### القرار
يُزال ادعاء «100% توافق مع نظام العمل» من الموقع العام، امتداداً لـDEC-007 المعلق. وتُزال عبارة «يتم الاحتساب تلقائياً حسب نظام العمل السعودي» من صفحة التصفية. ويُضاف في صفحات التصفية والجزاءات نص: «أداة مساعدة، والقرار النظامي مسؤولية المنشأة». وتُصاغ شروط خدمة تحدد المسؤولية، خصوصاً إن اعتُمد تموضع «المكتب الخلفي المُدار» (E) في DEC-001.

### السبب
الأدلة الموثقة تنقض الادعاء. وفي تموضع E ينفّذ رديف الحسومات بنفسه، فيصبح طرفاً في المسؤولية.

### الأدلة
- docs/council/EXECUTION_LOG.md (بند DEC-007 معلق)
- src/app/settlements/new/page.tsx

### الفريق الأحمر
الفريق 1: التعرض الأكبر في ادعاء الموقع، لا في نص صفحة التصفية.

### طريقة التحقق
البحث بـgrep عن «حسب نظام العمل» في src/app لا يُرجع ادعاءً مطلقاً، ومراجعة الموقع تمت.

---

## DOM-018 — البنية المعمارية للمستندات

**يحتاج موافقة المالك:** نعم · **الثقة:** MEDIUM

### القرار
قبل أي بند يتطلب PDF من الخادم (القسيمة، ومحضر التحقيق، وحزمة الدعوى، وشهادة الخدمة) يُتخذ قرار معماري واحد. البدائل: محرك مشترك، أو خدمة خارج عمليات المستأجرين، أو قالب طباعة CSS موحّد كحل مرحلي. يُبدأ بالطباعة (UX-19)، ويُرفع كل بند PDF إلى جهد M على الأقل.

### السبب
package.json لا يحتوي أي مكتبة PDF. ووجود عملية مستقلة لكل مستأجر بحجم 300 إلى 400 ميجابايت (DEC-004) يجعل إضافة Chromium قراراً معمارياً. وDATA-23 يتعارض مع UX-19.

### الأدلة
- package.json (exceljs, xlsx, qrcode فقط)

### الفريق الأحمر
الفريق 2.

### طريقة التحقق
وثيقة قرار معماري قبل أي تنفيذ لـPDF.

---

## DOM-019 — القائمة والصلاحيات

**يحتاج موافقة المالك:** نعم · **الثقة:** MEDIUM

### القرار
مواءمة القائمة مع الصلاحيات الفعلية (UX-07) قرار للمالك: هل يرى HR التحقيقات؟ وهل يرى المدير المالي المسير والتصفيات؟ وهل يرى المالك الوحدات القانونية للقراءة؟ التنفيذ يعالج أيضاً صفوف RolePermission المخصصة لدى المستأجرين، التي تلغي defaultRoles، ويغيّر افتراضي canAccessPath إلى «رفض ما لم توجد قاعدة» مع اختبار يقارنه بحارس الـAPI. في موجة 0 يُضاف فقط وصول PURCHASING_AGENT إلى /incoming-requests بنطاق ASSET_REQUEST.

### السبب
الصفحة المخفية تبدو للعميل ميزة غير موجودة. لكن تغيير القائمة وحده لا يعمل عند المستأجر الذي خصّص صلاحياته.

### الأدلة
- src/lib/menu.ts:38,339,389-393,576-577
- src/app/api/incoming-requests/route.ts:64,119,173,596

### الفريق الأحمر
الفريق 2: الاختبار المقترح في UX-07 دائري، وإضافة الدور إلى HUB_ROLES تكشف له السلف والخصومات.

### طريقة التحقق
استعلام عدّ لصفوف RolePermission المخصصة لكل مستأجر قبل النشر.

---

## DOM-020 — الأسطول والعهد

**يحتاج موافقة المالك:** لا · **الثقة:** MEDIUM

### القرار
يُقتصر في الأسطول والعهد على ما يمنع ضرراً مالياً موثّقاً. الآن، بلا مخطط: توحيد رقم اللوحة عند فحص التكرار، وألا يتجاوز مجموع نسبتي الخطأ 100، والسبب إلزامي للإتلاف مع تسجيله في التدقيق. بعد دفعة المخطط: purchaseCost وserialNumber على الأصل، وdriverId على المطالبة. في COULD: VehicleAssignment الكامل، ورخصة القيادة، وQR، والصيانة. وفي DO_NOT: GPS، والوقود، والورشة، ومشتريات ERP.

### السبب
الأصل بلا قيمة، فلا يمكن حسم دين العمل (م88) ولا التعويض (م91). والمطالبة تفقد سائقها. أما بقية البنود فتوسّع نحو منتج أسطول لم يثبت أن العملاء يستخدمونه.

### الأدلة
- src/app/api/vehicles/route.ts:52-55
- src/app/api/claims/_lib.ts:15-26,37-46
- src/app/api/assets/_lib.ts:43-57

### الفريق الأحمر
الفريق 1.

### الخلافات
لجنة اللوجستيات صنّفت LOGISTICS-07 وLOGISTICS-09 وLOGISTICS-10 كلها MUST.

### طريقة التحقق
POST مطالبة بنسبتين 75/75 يُرد عليه بـ400. POST مركبة بلوحة 'LF أ ب ج1234' بعد 'LF أ ب ج 1234' يُرد عليه بـ409.

---

